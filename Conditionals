Conditionals 

Using the let keyword declare a variable named sale and assign it to true:

```js
let sale = true;
```


```js
const nums = [10, 40, 62]
const newNums = nums.map(theFunc)

function theFunc(num) {
  return num + 100;
}
```
